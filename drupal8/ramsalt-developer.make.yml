# Ramsalt D8 base makefile
# ----------------

api: 2
core: "8.x"

includes:
  # This will import the main drupal project and the base modules/themes/libraries.
  - https://raw.githubusercontent.com/ramsalt/DrupalMakes/master/drupal8/ramsalt-base.make.yml
  # And then we add the developer-only modules
  - https://raw.githubusercontent.com/ramsalt/DrupalMakes/master/drupal8/commons/drupal-devel.yml

defaults:
  projects:
    subdir: "contrib"
